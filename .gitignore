# Dependencies
node_modules/

# Build output
dist/

# NOTE: docs/data.js is intentionally committed — it is the dataset served by
# the public GitHub Pages site (contains real, but publicly-shared, guild data).

# IDE / editor state
.idea/
.vscode/
*.swp
.DS_Store

# Local Claude settings (machine-specific paths & permissions)
.claude/settings.local.json
